import { useEffect ,useState } from "react";

function Content(){
    const [countdown , setCountdown] = useState(180)
    useEffect(() => {
       
    },[])

    return(
        <div>
        <h1>{changeWidth}</h1>
        </div>

    )
}
export default Content;